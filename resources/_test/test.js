function test (a,b,c) 
{
	var person = {
		firstName: "John",
		lastName : "Doe",
		id       : 5566,
		wholeName : function(fn, ln) {
//			fn = ln.m();
//			var fn;
//			fn = a;
			return fn + " " + ln;
		}
	};
	m(a,c);
	b = c;
	var y = person.lastName;
	var z = person.wholeName(x, y);
	z = person.firstName + y + a;
	c = [];
	c = person[1] + person[fn];
	if ( a || c ) {
		person.lastName = z;
	}
}
